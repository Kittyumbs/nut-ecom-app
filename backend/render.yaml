services:
  - type: web
    name: nut-ecom-backend
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: FIREBASE_SERVICE_ACCOUNT_KEY
        sync: false  # Set this in Render dashboard
    healthCheckPath: /health

