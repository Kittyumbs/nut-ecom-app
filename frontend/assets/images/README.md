# Assets Images

Đặt logo và các hình ảnh của app vào thư mục này.

## Logo
- `logo.svg` - Logo chính của ứng dụng

## Cách sử dụng
Logo có thể được sử dụng trong app thông qua widget `AppLogo` hoặc trực tiếp với `SvgPicture.asset()`.

